## Depencencies for Mappo modules

In this directory put every dependency for every module.

Files in lib are loaded first of their sibilings.

In general it should be better to create or use a smart package.